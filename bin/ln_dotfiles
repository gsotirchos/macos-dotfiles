#!/usr/bin/env bash

DOTFILES="$1"

# make soft symlinks of dotfiles
for filename in ${DOTFILES}/*; do
    if [[ -f "${filename}" ]]; then
        ln -sfv "${filename}" "${HOME}/.$(basename ${filename})"
    fi
done
